# Only needed for production builds where the frontend and backend are NOT
# same-origin (e.g. frontend on Cloudflare Pages, backend on Cloud Run).
# Leave unset for local dev — Vite's proxy in vite.config.ts handles /api.
VITE_API_BASE_URL=https://your-backend.example.com/api
